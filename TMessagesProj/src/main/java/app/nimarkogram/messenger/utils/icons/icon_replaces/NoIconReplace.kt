/**
 * Ported from Cherrygram (uz.unnarsx.cherrygram.core.icons.icon_replaces.NoIconReplace).
 * Originally Copyright github.com/arsLan4k1390, 2022-2026. GPL v2+.
 */

package app.nimarkogram.messenger.utils.icons.icon_replaces

import app.nimarkogram.messenger.utils.icons.newHashMap

class NoIconReplace : BaseIconReplace() {
    override val replaces: HashMap<Int, Int> = newHashMap()
}
